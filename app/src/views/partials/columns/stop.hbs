<div uk-sortable="group: sortable-group{{#unless @first}}-previous{{/unless}}" data-sprint={{_id}} data-group="stop" {{#noSortableHlp @first ended user.admin}}class="uk-sortable-nodrag"{{/noSortableHlp}}>
	{{#each stop}}
		{{> ../card}}
	{{/each}}
</div>