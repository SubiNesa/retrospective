<div uk-sortable="group: sortable-group{{#unless @first}}-previous{{/unless}}" data-sprint={{_id}} data-group="continue" {{#noSortableHlp @first ended user.admin}}class="uk-sortable-nodrag"{{/noSortableHlp}}>
	{{#each continue}}
		{{> ../card this}}
	{{/each}}
</div>